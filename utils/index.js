const term = require('')

module.exports = {
  commandType: (input) => {
    if (input === '') {
      return 'Please enter a valid command';
    }
    const command = input.split(' ')

    if
    console.log('command:', command)
  },
}